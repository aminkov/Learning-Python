#every function returns value
#return exits the function

def pow(a):
    return a*a
    print('log:the function completed successfully') #this line will never be executed - return exits the function

print(pow(3) + pow(5)) 
